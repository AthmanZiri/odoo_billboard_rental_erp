<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Sale Order Report Customization -->
    <template id="report_saleorder_document_inherit_media" inherit_id="sale.report_saleorder_document">
        <!-- Add Custom CSS -->
        <xpath expr="//t[@t-call='web.external_layout']" position="inside">
            <style>
                .media-specs {
                    font-size: 0.90rem;
                    line-height: 1.1;
                    color: #555;
                    margin-top: 2px;
                    border-top: 1px solid #eee;
                    padding-top: 2px;
                }
                .media-spec-item {
                    display: block;
                    margin-bottom: 0px;
                }
                
                table.o_main_table tbody tr td {
                    padding-top: 0px !important;
                    padding-bottom: 0px !important;
                    vertical-align: middle !important;
                    font-size: 0.90rem !important;
                }
                .media-spec-label {
                    font-weight: bold;
                }

                .text-red-total, .o_total td:last-child, .o_total td:nth-child(2) {
                    color: #e62117 !important;
                    font-weight: bold;
                }
                
                /* Red Table Headers */
                table.o_main_table thead tr th {
                    background-color: #b11f18 !important;
                    color: white !important;
                    text-transform: uppercase;
                    border: 1px solid #b11f18 !important;
                }

                /* Fix wkhtmltopdf bug where flex containers at the top of a new page ignore margin-top */
                div.clearfix, div#total, .row {
                    page-break-inside: avoid !important;
                }
            </style>
        </xpath>

        <!-- Custom Table Header -->
        <xpath expr="//table[contains(@class, 'o_main_table')]//thead//tr" position="replace">
            <tr>
                <th name="th_item" class="text-start">ITEM</th>
                <th name="th_billboard" class="text-start">Description</th>
                <th name="th_priceunit" class="text-end">RATE</th>
                <th name="th_quantity" class="text-end">QUANTITY</th>
                <th name="th_subtotal" class="text-end">Sub- Total</th>
            </tr>
        </xpath>

        <!-- Custom Table Body -->
        <xpath expr="//table[contains(@class, 'o_main_table')]//tbody" position="replace">
            <tbody class="sale_tbody">
                <t t-set="current_subtotal" t-value="0"/>
                <t t-foreach="lines_to_report" t-as="line">
                    <t t-set="current_subtotal" t-value="current_subtotal + line.price_subtotal"/>
                    <tr t-att-class="'bg-200 font-weight-bold o_line_section' if line.display_type == 'line_section' else 'font-italic o_line_note' if line.display_type == 'line_note' else ''">
                        <t t-if="not line.display_type">
                            <td name="td_item">
                                <span t-field="line.product_id.name"/>
                            </td>
                            <td name="td_name">
                                <span t-field="line.name" t-options="{'widget': 'text'}"/>
                            </td>
                            <td name="td_priceunit" class="text-end">
                                <span t-field="line.price_unit"/>
                            </td>
                            <td name="td_quantity" class="text-end">
                                <span t-field="line.product_uom_qty"/>
                                <span t-field="line.product_uom_id"/>
                            </td>
                            <td name="td_subtotal" class="text-end o_price_total">
                                <span t-field="line.price_subtotal"/>
                            </td>
                        </t>
                        <t t-elif="line.display_type == 'line_section'">
                            <td name="td_section_line" colspan="99">
                                <span t-field="line.name"/>
                            </td>
                            <t t-set="current_section" t-value="line"/>
                            <t t-set="current_subtotal" t-value="0"/>
                        </t>
                        <t t-elif="line.display_type == 'line_note'">
                            <td name="td_note_line" colspan="99">
                                <span t-field="line.name"/>
                            </td>
                        </t>
                    </tr>
                </t>
            </tbody>
        </xpath>
    </template>

    <!-- Invoice Report Customization -->
    <template id="report_invoice_document_inherit_media" inherit_id="account.report_invoice_document">
        <!-- Add Custom CSS -->
        <xpath expr="//t[@t-call='web.external_layout']" position="inside">
            <style>
                .media-specs {
                    font-size: 0.80rem;
                    line-height: 1.1;
                    color: #555;
                    margin-top: 2px;
                    border-top: 1px solid #eee;
                    padding-top: 2px;
                }
                .media-spec-item {
                    display: block;
                    margin-bottom: 0px;
                }
                
                table.o_main_table tbody tr td {
                    padding-top: 0px !important;
                    padding-bottom: 0px !important;
                    vertical-align: middle !important;
                    font-size: 0.90rem !important;
                }
                .media-spec-label {
                    font-weight: bold;
                }

                .text-red-total, .o_total td:last-child, .o_total td:nth-child(2) {
                    color: #e62117 !important;
                    font-weight: bold;
                }
                
                /* Red Table Headers */
                table.o_main_table thead tr th {
                    background-color: #b11f18 !important;
                    color: white !important;
                    text-transform: uppercase;
                    border: 1px solid #b11f18 !important;
                }

                /* Fix wkhtmltopdf bug where flex containers at the top of a new page ignore margin-top */
                div.clearfix, div#total, .row {
                    page-break-inside: avoid !important;
                }
            </style>
        </xpath>

        <!-- Custom Table Header -->
        <xpath expr="//table[contains(@class, 'o_main_table')]//thead//tr" position="replace">
            <tr>
                <th name="th_item" class="text-start">ITEM</th>
                <th name="th_billboard" class="text-start">Description</th>
                <th name="th_priceunit" class="text-end">RATE</th>
                <th name="th_quantity" class="text-end">QUANTITY</th>
                <th name="th_subtotal" class="text-end">Sub- Total</th>
            </tr>
        </xpath>

        <!-- Custom Table Body -->
        <xpath expr="//table[contains(@class, 'o_main_table')]//tbody" position="replace">
            <tbody class="invoice_tbody">
                <t t-set="current_subtotal" t-value="0"/>
                <t t-foreach="o.invoice_line_ids" t-as="line">
                    <t t-set="current_subtotal" t-value="current_subtotal + line.price_subtotal"/>

                    <tr t-att-class="'bg-200 font-weight-bold o_line_section' if line.display_type == 'line_section' else 'font-italic o_line_note' if line.display_type == 'line_note' else ''">
                        <t t-if="line.display_type == 'product' or not line.display_type">
                            <td name="td_item">
                                <span t-field="line.product_id.name"/>
                            </td>
                            <td name="td_name">
                                <span t-field="line.name" t-options="{'widget': 'text'}"/>
                            </td>
                            <td name="td_priceunit" class="text-end">
                                <span t-field="line.price_unit"/>
                            </td>
                            <td name="td_quantity" class="text-end">
                                <span t-field="line.quantity"/>
                                <span t-field="line.product_uom_id"/>
                            </td>
                            <td name="td_subtotal" class="text-end o_price_total">
                                <span t-field="line.price_subtotal"/>
                            </td>
                        </t>
                        <t t-elif="line.display_type == 'line_section'">
                            <td name="td_section_line" colspan="99">
                                <span t-field="line.name" t-options="{'widget': 'text'}"/>
                            </td>
                            <t t-set="current_section" t-value="line"/>
                            <t t-set="current_subtotal" t-value="0"/>
                        </t>
                        <t t-elif="line.display_type == 'line_note'">
                            <td name="td_note_line" colspan="99">
                                <span t-field="line.name" t-options="{'widget': 'text'}"/>
                            </td>
                        </t>
                    </tr>
                </t>
            </tbody>
        </xpath>
    </template>
</odoo>
